<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.SystemConfigMapper">

    <resultMap id="BaseResultMap" type="org.example.entity.SystemConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="config_type" property="configType"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM system_config WHERE id = #{id}
    </select>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT * FROM system_config WHERE config_key = #{configKey}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM system_config ORDER BY id
    </select>

    <insert id="insert" parameterType="org.example.entity.SystemConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO system_config (config_key, config_value, config_type, description)
        VALUES (#{configKey}, #{configValue}, #{configType}, #{description})
    </insert>

    <update id="update" parameterType="org.example.entity.SystemConfig">
        UPDATE system_config
        <set>
            <if test="configValue != null">
                config_value = #{configValue},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM system_config WHERE id = #{id}
    </delete>

</mapper>
